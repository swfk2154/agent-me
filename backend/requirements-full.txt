# agent-me full deps (vector memory + file analysis, ~400MB)
# adds: ChromaDB ONNX embedding + PDF/DOCX parsing + file RAG

-r requirements.txt

chromadb==0.5.23
pdfplumber==0.11.4
python-docx==1.1.2
PyPDF2==3.0.1
